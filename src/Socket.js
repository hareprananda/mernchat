import React from 'react'
import io from "socket.io-client";
const Socket = () => {
    var socket = io();
    socket.on
    return (
        <div>
            
        </div>
    )
}

export default Socket
